{
  "name": "keblog",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "python3 server/content_server.py --dev",
    "build": "node scripts/build.mjs",
    "test": "python3 -m unittest discover -s tests -v",
    "start": "python3 server/content_server.py"
  },
  "description": "Git-backed personal website and paper reading archive"
}
